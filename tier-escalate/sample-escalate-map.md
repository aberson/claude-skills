# tier-escalate escalation map — sample output

> Sample produced by `/tier-escalate` against a 42-skill `.claude/skills/` set (2026-07-09).
> 3 FABLE-SEED skills and 3 conditional escalations found; the other 36 skills stay on
> Opus orchestrating + Sonnet fan-out. This is a realistic example of the skill's artifact.

A read of all 42 workspace skills, classified against the up verdict rule (escalate a *session*
to Fable 5 only for a single load-bearing seed-artifact — a phase whose one output is the
deliverable's quality ceiling AND where a diversity committee cannot substitute for a stronger
single reasoner). The three up-corrections were applied: (a) fan-out arms are never Fable —
diversity beats per-arm strength; (b) single-mind synthesis is a candidate for escalation, not
parallelization; (c) every conditional escalation names its concrete trigger.

## The Fable-worthy table — only these three skills have a seed-artifact phase

| Skill | Seed-artifact phase | Why a committee can't substitute | How to apply |
|---|---|---|---|
| plan-init | the plan.md greenfield-architecture authoring pass | the seed the whole build pipeline inherits — reviewers downstream polish it but can't raise its ceiling. Skip if front-loading a dense, fully-decided spec (then it's just rendering; Opus is fine) | start the session with `/model claude-fable-5`, run the authoring pass, drop back after |
| user-debug | Step 1 root-cause diagnosis on the hard tail | the built-in independent repro is a *check*, not a stronger reasoner — two Opus agents can converge on the same wrong root cause | start the session with `/model claude-fable-5`, run the diagnosis, drop back after |
| deep-research | the final cited-report synthesis | fan-out search/verify arms stay cheap; the synthesis must hold one coherent cited argument across every source — no committee produces that voice | start the session with `/model claude-fable-5`; the search/verify arms stay Sonnet |

## Conditional escalations — Opus default, Fable on a named trigger

| Skill | Trigger | Phase | How to apply |
|---|---|---|---|
| plan-feature | a large cross-cutting feature | the feature-plan authoring pass | `/model claude-fable-5` for the session when the trigger holds |
| plan-merge | a deep-conflict merge of 3+ overlapping plans | the overlap/conflict analysis | `/model claude-fable-5` for the session when the trigger holds |
| review-deep | a high-stakes substrate/schema/wire-shape diff | the Bugs lens | `/review-deep --model-override bugs=fable` (a flag, not a session switch) — pending #289 (the flag's tier enum is `haiku\|sonnet\|opus` today); until it lands, use `/model claude-fable-5` for the session |

## Everything else stays Opus orchestrating + Sonnet fan-out (36 skills)

- **review-gauntlet, review-deep (default runs), review-proof, review-uat** — multi-lens
  reviewer committees; diversity + iteration carries quality, a stronger single model adds
  cost, not ceiling.
- **memory-distill** — the final latent-principle consolidation is single-mind synthesis
  (never fan it out — up-correction b), but the 2026-07-09 scan found Fable slightly *worse*
  there, not better; stays Opus.
- **build-step, build-phase, build-queue** — up-to-3 dev retries + reviewer committees are
  the quality mechanism; escalating arms multiplies cost with no ceiling gain.
- **skill-iterate, skill-evolve, skill-eval-setup** — grader arrays and parallel A/B variants;
  N-judge voting carries quality.
- **repo-init, repo-sync, repo-update** — templated docs/issues/git work; Opus is already
  above the difficulty ceiling.
- **user-brainstorm, user-learn, user-draft, user-pm, user-orient, user-recap** — user-brainstorm
  and user-learn fan out *independent parallel writes* with no cross-synthesis ceiling (no
  single seed artifact); the rest are single-pass work Opus handles at ceiling.
- **plan-review, plan-wrap, plan-trim, plan-expedite** — checklist single-pass reasoning; no
  seed-artifact shape.
- Trivial slices → Sonnet/Haiku per the standing policy.

## Paste-ready CLAUDE.md escalation list

> **PASTE-READY — regenerated by `/tier-escalate`, never auto-applied.** Diff this against the
> escalation sentences in CLAUDE.md's `## Environment` model paragraph (the policy owner) and
> paste only if the live paragraph has drifted. (Reproduced verbatim from the policy — note the
> `--model-override bugs=fable` form additionally needs review-deep's tier enum widened, #289.)

```text
Escalate a **session** to **Fable 5** (`claude-fable-5`) only for the single-seed-artifact
shape, where a diversity committee can't substitute for a stronger reasoner: `/plan-init`
greenfield-architecture authoring, `/user-debug` hard root-cause diagnosis, `/deep-research`
cited-report synthesis. Conditional escalations: a large cross-cutting `/plan-feature`, a
deep-conflict 3+ `/plan-merge`, a high-stakes substrate/schema diff
(`/review-deep --model-override bugs=fable`).
```

---

## Next (per-session, NOT done by tier-escalate)

For a **FABLE-SEED** skill: start the session with `/model claude-fable-5`, run the seed phase,
drop back to the pinned default after. For a **CONDITIONAL** skill: escalate only when its named
trigger holds. Practical constraints (retention, `/model` pin resets) live in the CLAUDE.md model
paragraph this map is derived from. This skill does NOT switch models — it only maps where
switching pays.
